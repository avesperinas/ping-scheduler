[project]
name = "ping-scheduler"
version = "0.1.0"
description = "Schedule a recurring CLI invocation so your session window opens when you choose"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.0",
    "uvicorn[standard]>=0.52.0",
    "sqlalchemy>=2.0.0",
    "apscheduler>=3.11.0",
    "tzdata>=2026.1",
]
